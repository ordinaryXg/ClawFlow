# ClawFlow DevLog - 2026-05-07

## TL;DR

- 完成任务 2.6：仪表盘页面重构（Ant Design 卡片化 + 指标 + 快捷入口）
- Gateway 启停对接 `gatewayStore`，并在启停后刷新状态，CLI 缺失时禁用操作并提示
- `npm run lint/typecheck` 当前会被 `out/` 目录的声明文件与部分配置文件告警/报错影响（非本次改动引入）
- **约定**：HTML 产品原型统一放在 `${REPO_ROOT}/AICodeFile/prototype/`（入口 `index.html`）；索引、PRD、原型 README 已同步路径说明

## 变更内容

- **功能/模块**：Dashboard（仪表盘）
- **涉及文件**：
  - `src/pages/DashboardPage.tsx`
  - `AICodeFile/06_TASKS.md`（补充 2.6 完成记录与修正文件路径）
  - `AICodeFile/00_INDEX.md`、`AICodeFile/03_PRD.md`、`AICodeFile/prototype/README.md`（原型目录约定与路径）
- **接口/协议变化**：无（复用现有 `electronAPI` 与 store）

## 决策与权衡

- **为什么禁用“设置”入口按钮**：当前路由未接入 SettingsPage，禁用态比跳转报错更符合用户预期；后续任务 2.7 完成后再打开。
- **为什么启停后强制刷新状态**：避免“仅靠本地状态置 running/stopped”导致与真实 Gateway 状态不一致。

## 验证方式

- [x] 手动：打开 `/dashboard`，观察指标卡片、Gateway 状态标签与启停按钮、快捷入口跳转
- [x] Lint/Typecheck：已尝试执行；当前仓库存在与 `out/` 相关的 lint 报错导致命令失败（建议后续将 `out/` 排除出 lint 目标）

## 已知问题 / 后续工作

- `npm run lint` 会扫描 `out/` 目录导致大量 `import/no-unresolved` 报错；建议在 ESLint 配置中忽略 `out/` 或仅 lint `src/`。
